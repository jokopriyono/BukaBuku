package id.ac.budiluhur.bukabuku.network;

import retrofit2.http.Body;
import retrofit2.http.GET;
import retrofit2.http.POST;
import retrofit2.http.Path;
import retrofit2.http.Query;
import rx.Observable;


public interface ApiServices {
    /*@GET("volumes")
    Observable<Books> getTopBooks(@Query("q") String key);

    @GET("volumes/{id}")
    Observable<Item> getDetailBook(@Path("id") String id);

    @POST("signin")
    Observable<SignInResult> loginUser(@Body SignInBody body);

    @POST("register")
    Observable<RegisterResult> registerUser(@Body RegisterBody body);

    @POST("activation")
    Observable<ActivationResult> aktivasiUser(@Body ActivationBody body);*/
}